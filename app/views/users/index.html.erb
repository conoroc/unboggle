<% provide(:title, 'All users') %>
<div class="well opaque span6 offset3"  id="gmapdiv" data-user="<%= User.limit(7).to_json %>" >
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>
  <div id="my_google_map"></div>

<%= will_paginate %>
</div>

